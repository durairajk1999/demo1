import { FundTransferResponse } from './fund-transfer-response';

describe('FundTransferResponse', () => {
  it('should create an instance', () => {
    expect(new FundTransferResponse()).toBeTruthy();
  });
});
