import { EncryptionAndDecryption } from './encryption-and-decryption';

describe('EncryptionAndDecryption', () => {
  it('should create an instance', () => {
    expect(new EncryptionAndDecryption()).toBeTruthy();
  });
});
