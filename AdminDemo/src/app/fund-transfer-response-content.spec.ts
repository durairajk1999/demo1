import { FundTransferResponseContent } from './fund-transfer-response-content';

describe('FundTransferResponseContent', () => {
  it('should create an instance', () => {
    expect(new FundTransferResponseContent()).toBeTruthy();
  });
});
