import { MerchantWalletBalance } from './merchant-wallet-balance';

describe('MerchantWalletBalance', () => {
  it('should create an instance', () => {
    expect(new MerchantWalletBalance()).toBeTruthy();
  });
});
