import { MenuOrderingTableResponse } from './menu-ordering-table-response';

describe('MenuOrderingTableResponse', () => {
  it('should create an instance', () => {
    expect(new MenuOrderingTableResponse()).toBeTruthy();
  });
});
