import { WalletHistoryRequest } from './wallet-history-request';

describe('WalletHistoryRequest', () => {
  it('should create an instance', () => {
    expect(new WalletHistoryRequest()).toBeTruthy();
  });
});
