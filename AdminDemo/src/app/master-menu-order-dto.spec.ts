import { MasterMenuOrderDTO } from './master-menu-order-dto';

describe('MasterMenuOrderDTO', () => {
  it('should create an instance', () => {
    expect(new MasterMenuOrderDTO()).toBeTruthy();
  });
});
