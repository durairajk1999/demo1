import { ShgDetailsRequest } from './shg-details-request';

describe('ShgDetailsRequest', () => {
  it('should create an instance', () => {
    expect(new ShgDetailsRequest()).toBeTruthy();
  });
});
