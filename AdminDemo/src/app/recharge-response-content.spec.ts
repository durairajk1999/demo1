import { RechargeResponseContent } from './recharge-response-content';

describe('RechargeResponseContent', () => {
  it('should create an instance', () => {
    expect(new RechargeResponseContent()).toBeTruthy();
  });
});
