import { StatementResponseContent } from './statement-response-content';

describe('StatementResponseContent', () => {
  it('should create an instance', () => {
    expect(new StatementResponseContent()).toBeTruthy();
  });
});
