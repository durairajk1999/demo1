import { MasterMenuSetting } from './master-menu-setting';

describe('MasterMenuSetting', () => {
  it('should create an instance', () => {
    expect(new MasterMenuSetting()).toBeTruthy();
  });
});
