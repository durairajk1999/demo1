import { MenusOrderingResponseContent } from './menus-ordering-response-content';

describe('MenusOrderingResponseContent', () => {
  it('should create an instance', () => {
    expect(new MenusOrderingResponseContent()).toBeTruthy();
  });
});
