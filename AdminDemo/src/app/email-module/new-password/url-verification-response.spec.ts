import { UrlVerificationResponse } from './url-verification-response';

describe('UrlVerificationResponse', () => {
  it('should create an instance', () => {
    expect(new UrlVerificationResponse()).toBeTruthy();
  });
});
