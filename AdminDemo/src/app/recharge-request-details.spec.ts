import { RechargeRequestDetails } from './recharge-request-details';

describe('RechargeRequestDetails', () => {
  it('should create an instance', () => {
    expect(new RechargeRequestDetails()).toBeTruthy();
  });
});
