import { AepsMatmResponseDetails } from './aeps-matm-response-details';

describe('AepsMatmResponseDetails', () => {
  it('should create an instance', () => {
    expect(new AepsMatmResponseDetails()).toBeTruthy();
  });
});
