import { TransactionDetailsMerchant } from './transaction-details-merchant';

describe('TransactionDetailsMerchant', () => {
  it('should create an instance', () => {
    expect(new TransactionDetailsMerchant()).toBeTruthy();
  });
});
