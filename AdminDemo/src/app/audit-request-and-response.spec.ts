import { AuditRequestAndResponse } from './audit-request-and-response';

describe('AuditRequestAndResponse', () => {
  it('should create an instance', () => {
    expect(new AuditRequestAndResponse()).toBeTruthy();
  });
});
