import { ResponseMerchantReport } from './response-merchant-report';

describe('ResponseMerchantReport', () => {
  it('should create an instance', () => {
    expect(new ResponseMerchantReport()).toBeTruthy();
  });
});
