import { TransactionMerchantRequest } from './transaction-merchant-request';

describe('TransactionMerchantRequest', () => {
  it('should create an instance', () => {
    expect(new TransactionMerchantRequest()).toBeTruthy();
  });
});
