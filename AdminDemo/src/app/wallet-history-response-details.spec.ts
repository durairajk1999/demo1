import { WalletHistoryResponseDetails } from './wallet-history-response-details';

describe('WalletHistoryResponseDetails', () => {
  it('should create an instance', () => {
    expect(new WalletHistoryResponseDetails()).toBeTruthy();
  });
});
