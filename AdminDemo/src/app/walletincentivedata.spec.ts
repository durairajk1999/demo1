import { Walletincentivedata } from './walletincentivedata';

describe('Walletincentivedata', () => {
  it('should create an instance', () => {
    expect(new Walletincentivedata()).toBeTruthy();
  });
});
