import { MerchantTransaction } from './merchant-transaction';

describe('MerchantTransaction', () => {
  it('should create an instance', () => {
    expect(new MerchantTransaction()).toBeTruthy();
  });
});
