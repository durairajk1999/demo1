import { FundTransferRequest } from './fund-transfer-request';

describe('FundTransferRequest', () => {
  it('should create an instance', () => {
    expect(new FundTransferRequest()).toBeTruthy();
  });
});
