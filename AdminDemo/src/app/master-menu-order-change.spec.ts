import { MasterMenuOrderChange } from './master-menu-order-change';

describe('MasterMenuOrderChange', () => {
  it('should create an instance', () => {
    expect(new MasterMenuOrderChange()).toBeTruthy();
  });
});
