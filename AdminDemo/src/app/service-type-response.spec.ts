import { ServiceTypeResponse } from './service-type-response';

describe('ServiceTypeResponse', () => {
  it('should create an instance', () => {
    expect(new ServiceTypeResponse()).toBeTruthy();
  });
});
