import { Walletbalancerequest } from './walletbalancerequest';

describe('Walletbalancerequest', () => {
  it('should create an instance', () => {
    expect(new Walletbalancerequest()).toBeTruthy();
  });
});
