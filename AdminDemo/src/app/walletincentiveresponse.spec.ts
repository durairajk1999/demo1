import { Walletincentiveresponse } from './walletincentiveresponse';

describe('Walletincentiveresponse', () => {
  it('should create an instance', () => {
    expect(new Walletincentiveresponse()).toBeTruthy();
  });
});
